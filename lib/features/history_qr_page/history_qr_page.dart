import 'package:flutter/material.dart';

class HistoryQrPage extends StatelessWidget {
  const HistoryQrPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("comming soon"),
      ),
    );
  }
}
