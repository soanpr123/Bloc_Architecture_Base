import 'package:flutter/material.dart';

class AmaiStorePage extends StatelessWidget {
  const AmaiStorePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("amai _store"),
      ),
    );
  }
}
